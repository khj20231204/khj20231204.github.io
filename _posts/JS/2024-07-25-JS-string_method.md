---
layout: single
title: String메소드
categories: JS
tag: []
---
 
1. # String메소드
   ```js
      var msg = new String("Everything is practice");

      msg.substring(7, 10);
      msg.slice(7, 10);
      msg.slice(-3);
      msg.indexOf("t");
      msg.indexOf("i",10);
      msg.lastIndexOf("t");
      msg.split(" ");
      msg.replace("i","I");
      msg.toUpperCase();
      msg.endsWith(".");
   ```
   


